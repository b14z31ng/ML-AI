services:
  backend:
    build: ./backend
    ports:
      - "8000:10000"
    volumes:
      - ./backend/artifacts:/app/artifacts
    environment:
      - PYTHONUNBUFFERED=1

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
